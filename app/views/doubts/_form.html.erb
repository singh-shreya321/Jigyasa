<%= form_for([@course, @course.doubts.build]) do |f| %>
  <p>
    <%= f.label :Title %>
    <br>
    <%=f.text_area :title %>
  </p>
  <p>
    <%= f.label :Body %>
    <br>
    <%=f.text_area :body %>
  </p>
  <br>
  <%= f.submit %>
<% end %>
